# 更新日志

本项目遵循 [语义化版本](https://semver.org/lang/zh-CN/)。

## [v1.0.0-rc3] - 2026-09-17

### 修复
- 同步 `vma_select.h` 声明与实现，改为数组形式：
  - `to_vma_select(const char **s, int count)`
  - `free_vma_select(struct vma_select **select, int count)`
  - `vma_filter_select(struct vm_area **area, struct vma_select **select, int count)`
- `vm_area` 的 `perm` 缓冲区从 4 字节扩展到 5 字节，修复 `perm[4] = '\0'` 越界写入。
- `vma_select` 在 `area->pathname` 为空时回退到 `area->module->pathname`，修复 `.bss` 段匹配不到的问题。

### 说明
这是 v1.0.0 的候选版本。请测试并报告问题。

## [v1.0.0-rc2] - 2026-09-17

### 许可证变更
本项目切换至 GNU 宽通用公共许可证第 3 版（LGPLv3），以便其他程序（包括闭源程序）可以链接本库，所以本人做出重大决策，本项目切换至 **GNU 宽通用公共许可证 3 版（LGPLv3）**。

- 本项目所有代码现在均按 LGPLv3 授权。
- 完整许可证文本见 `LICENSE`（GPLv3 全文）和 `COPYING.LESSER`（LGPLv3 附加条款）。
- 衍生作品必须遵守 LGPLv3 条款。
- 之前的版本仍适用其原始许可证。

## [v1.0.0-rc1] - 2026-09-16

### 更新
- 我现在添加了libelf支持, 现在可以通过调用vma_elf来实现查看ELF信息。每个段的段类型，相对于段的索引，段所有ELF模块都能支持。
- 我添加了关于vma_map的哈希表，这意味着你们可以通过一个地址来查看他的内存段了！
- 添加了vma_select, 也就是你们能够通过 libabc.so:.bss[0];rw-p来选择内存范围了！对于粗糙的vma_filter来说这准确了很多，不过，这是相对的。
- 移除了命令行界面，目前，我仍然在努力，相信不久我们会支持命令行参数。
- 版本号更新至1.x, 这是突破性的, 不过这是相对的。
- 我们为了支持libelf的使用, 将协议修改为了GPLv3, 这是重大且相对的!

### 许可证变更
此版本标志着本项目切换至 **GNU 通用公共许可证第 3 版（GPLv3）**。

- 本项目所有代码现在均按 GPLv3 授权。
- 完整许可证文本见 `LICENSE` 文件。
- 衍生作品必须遵守 GPLv3 条款。
- 之前的版本仍适用其原始许可证。

### 说明
这是 v1.0.0 的候选版本。请测试并报告问题。

## [v0.2.0] - 2026-08-27 6:00

### 新增
- **自定义比较回调 `memscanv_compar_t`**  
  `memscanv` 现支持可选的块匹配回调参数，允许用户在不修改扫描核心的情况下实现任意匹配逻辑（如范围匹配、多字符串 AC 自动机、模糊匹配等）。回调采用**块掩码**设计，一次调用处理整个内存块，大幅降低函数调用开销。
- **`userdata` 参数支持**  
  回调可通过 `void *userdata` 传递自定义上下文（如 AC 自动机状态、排序候选数组等），实现灵活的多模式匹配。

### 优化
- **单次扫描多模式匹配**  
  通过回调机制，`memscanv` 可在一次内存遍历中同时匹配多个模式，彻底摆脱之前对非整数模式逐个扫描的低效方式。
- **对齐处理**  
  回调扫描路径增加了地址对齐修正，确保候选地址始终按指定步长对齐，避免漏报或错误匹配。

### 修复
- 修复了回调扫描在单线程路径下错误释放调用者 `vm_area` 链表的问题（改为复制链表）。
- 修复了回调扫描缺少重叠读取的问题，确保跨块边界的模式能够被正确检测。
- 修复了 `process_reader` 返回非正值时推进步长未限制在区域范围内的问题。
- 修复了 `callback.h` 缺少 `<stdint.h>` 包含导致独立编译失败的问题。

## [v0.1.0] - 2026-08-26

### 新增
- **多值批量扫描函数 `memscanv`**  
  底层新增 `memscanv` 接口，支持一次扫描多个目标值（4 或 8 字节整数），大幅提升指针链扫描等高级应用的性能。
- **地址列表过滤增强**  
  扩展了 `addrlist_filter` 支持的范围，现在所有非快照过滤类型（包括浮点近似和整数近似）均可用。
- **快照管理改进**  
  添加 `free_addrlist_snapshot` 函数，方便释放快照内存，降低内存泄漏风险。

### 优化
- **多线程加速**  
  通用字节模式扫描（非 4/8 字节）也支持多线程并行，保留了原有的 `jobs` 参数加速能力。
- **地址列表去重**  
  过滤前始终对地址列表进行排序去重，避免重复结果。

### 修复
- 修正了 `addrlist_filter` 中过滤类型上限限制，浮点比较等类型不再被错误拒绝。
- 修复了 `src` 空指针检查逻辑，确保所有需要参考值的过滤器都正确验证参数。
- 移除了 `match_value_snap` 中多余的 `(void)delta;`。

## [v0.0.1] - 2026-07-31

### 初始版本
- 实现多线程内存扫描核心功能。
- 支持多种数据类型和比较操作。
- 提供区域过滤、地址列表过滤和快照差分分析。
- 命令行界面完成，支持保存/加载结果。